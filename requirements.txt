# lit-agent core dependencies.
# Policy (spec section 4): pure-pip, cross-platform only. Anything needing a JVM,
# a system package, or a GPU is an optional extra behind a capability flag.
#
# Verified on Python 3.14.2 / Windows (M0 spike S0). See docs/spikes/FINDINGS.md.

pymupdf>=1.24
httpx>=0.27
PyYAML>=6.0
bibtexparser>=1.4

# --- optional extras, installed only when the matching capability is enabled ---
# pymupdf4llm>=0.0.17    # layout_text: markdown-with-structure extraction.
#                        # NOT core - 85x slower than raw extraction for 3% more
#                        # text, and pulls pymupdf_layout + onnxruntime (~57 MB).
#                        # See docs/decisions/0001-fast-text-extraction-by-default.md
# sqlite-vec>=0.1.6      # vector_index (M9)
# ocrmypdf>=16           # scanned-PDF OCR; needs system Tesseract + Ghostscript
